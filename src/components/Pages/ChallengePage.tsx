import { Stack, Typography } from "@mui/material";

const ChallengePage = () => {
  return (
    <Stack>
      <Typography variant="h1">ChallengePage</Typography>
    </Stack>
  );
};

export default ChallengePage;
