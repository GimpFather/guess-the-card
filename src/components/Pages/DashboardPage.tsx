import { Typography } from "@mui/material";
import { FormattedMessage } from "react-intl";

const DashboardPage = () => {
  return (
    <Typography variant="h4" color="secondary">
      <FormattedMessage id="TEST_KEY" />
    </Typography>
  );
};

export default DashboardPage;
